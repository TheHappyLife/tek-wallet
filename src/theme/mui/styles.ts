import theme from "./theme";

export const sessionTitle = {
  color: "whiteText.main",
  fontWeight: theme.palette.fontWeight._600,
  fontSize: theme.palette.fontSize._16,
};

export const sessionDescription = {
  color: "whiteText._80",
  fontWeight: theme.palette.fontWeight._400,
  fontSize: theme.palette.fontSize._14,
  textAlign: "center",
};

export const headerTitle = {
  color: "whiteText.main",
  fontWeight: theme.palette.fontWeight._600,
  fontSize: theme.palette.fontSize._16,
};

export const noteContent = {
  color: "whiteText._80",
  fontWeight: theme.palette.fontWeight._400,
  fontSize: theme.palette.fontSize._12,
};
